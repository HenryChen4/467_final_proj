_target_: src.data_parsing.emotion_parsing.Emotion_Data_Parser

config:
  root_dir: "./data/"

  csv: "./main_data.csv"
  
  # for splitting
  train_split: 0.7
  test_split: 0.15
  val_split: 0.15
  seed: 1483
  max_samples: 200
  max_len: 200
  batch_size: 64
  